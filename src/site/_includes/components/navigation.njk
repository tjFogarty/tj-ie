<nav class="c-nav">
    <a class="c-nav__link" href="{{ '/' | url }}">TJ Fogarty</a>
    {%- for entry in collections.all | eleventyNavigation %}
    <a
        class="c-nav__link {% if entry.url == page.url %}c-nav__link--active{% endif %}"
        href="{{ entry.url | url }}"
        >{{ entry.title }}</a
    >
    {%- endfor %}
</nav>
