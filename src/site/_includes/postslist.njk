{% if postslistYears === 'off' %}
<ol class="post-list">
{% endif %}

{% set year = '' %}
  {% for post in postslist | reverse %}
    {% set postYear = post.date | year %}
    {% set isNewYear = year !== postYear %}
    {% if isNewYear and postslistYears !== 'off' %}

    {# this reminds me of working with ExpressionEngine #}
    {% if not loop.first %}
    </ol>
    </div>
    {% endif %}

    <div class="md:flex gap-5 post-list-item">
      <div class="relative">
        <h2 class="text-lg text-gray-500 sticky top-0">{{ postYear }}</h2>
      </div>

      <ol class="mb-8 pb-8 w-full border border-t-0 border-l-0 border-r-0 border-gray-200">
      {% set year = postYear %}
    {% endif %}
    <li class="mb-8 last:mb-0">
      {% include 'components/post-teaser.njk' %}
    </li>
  {% endfor %}
{% if postslistYears === 'off' %}
</ol>
{% endif %}
