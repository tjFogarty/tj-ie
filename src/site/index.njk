---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---
<article>
  <div class="flex items-center bg-rose-900 text-white p-8 min-h-[400px]">
    <div class="container mx-auto">
      <h1 class="text-5xl font-extrabold mb-4">I'm TJ</h1>
      <p class="text-2xl">
        A <strong>Front-End Software Engineer</strong> building performant and accessible solutions. <br>
        Specialising in JavaScript with React and Vue.
      </p>
    </div>
  </div>

  <div class="py-10">
    <div class="container">
      <h2 class="mb-8">Recent Posts</h2>

      {% set postslist = collections.posts | head(-3) %}
      {% set hlevel = '3' %}
      <ol class="mb-8">
        {% for post in postslist | reverse %}
        <li class="mb-5">
          {% include 'components/post-teaser.njk' %}
        </li>
        {% endfor %}
      </ol>

      <p>More posts can be found in <a href="{{ '/archive/' | url }}" class="decoration-rose-600 underline">the archive</a>.</p>
    </div>
  </div>

  <div class="bg-slate-200 py-10">
    <div class="container">
      <h2 class="mb-8">Photos</h2>

      {% set photolist = collections.photos | head(-6) %}
      <ul class="md:grid grid-cols-3 gap-12">
      {% for photo in photolist %}
        {% if photo.data.image %}
          <li class="mb-5 md:mb-0">
            <a href="{{ photo.url }}" class="block">
              <div class="thumbnail-container drop-shadow-xl aspect-square">
                {% photo photo.data.image, photo.data.description %}
              </div>
            </a>
          </li>
        {% endif %}
      {% endfor %}
      </ul>
    </div>
  </div>
    

</article>
